<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}StockMaster{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📦</text></svg>">

        {# ================================================================= #}
        {# 1. LES STYLES (CSS)                                               #}
        {# ================================================================= #}
        {% block stylesheets %}
            {# Thème Bootswatch ZEPHYR (Doux et moderne) #}
            <link rel="stylesheet" href="https://bootswatch.com/5/zephyr/bootstrap.min.css">

            {# FontAwesome pour les icônes #}
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

            <style>
                /* Sidebar : Couleur sombre "Pro" (#2c3e50) */
                #sidebar-wrapper {
                    min-height: 100vh;
                    min-width: 250px;
                    background-color: #2c3e50;
                    color: white;
                }

                /* Liens du menu */
                .list-group-item {
                    background-color: transparent;
                    color: rgba(255, 255, 255, 0.7); /* Blanc légèrement transparent */
                    border: none;
                    padding: 15px 20px;
                    transition: all 0.3s;
                    border-left: 4px solid transparent; /* Prépare la bordure au survol */
                }

                /* Effet au survol (Hover) */
                .list-group-item:hover {
                    background-color: rgba(255, 255, 255, 0.1);
                    color: white;
                    padding-left: 25px; /* Petit décalage vers la droite */
                    border-left: 4px solid #3498db; /* Petite barre bleue */
                }

                /* Logo StockMaster en haut à gauche */
                .sidebar-heading {
                    padding: 20px;
                    font-size: 1.5rem;
                    font-weight: bold;
                    background-color: rgba(0,0,0,0.2); /* Fond légèrement plus sombre */
                    text-align: center;
                    letter-spacing: 1px;
                }

                /* Fond de la page principale (Gris clair pour contraste) */
                #page-content-wrapper {
                    width: 100%;
                    background-color: #f8f9fa;
                }
            </style>
        {% endblock %}
    </head>
    <body>

        <div class="d-flex" id="wrapper">

            {# ================================================================= #}
            {# 2. LA BARRE LATÉRALE (SIDEBAR)                                    #}
            {# ================================================================= #}
            <div id="sidebar-wrapper">
                <div class="sidebar-heading">
                    <i class="fas fa-cubes me-2"></i>StockMaster
                </div>

                <div class="list-group list-group-flush my-3">

                    <a href="{{ path('app_dashboard') }}" class="list-group-item list-group-item-action">
                        <i class="fas fa-chart-line me-2"></i> Dashboard
                    </a>

                    <a href="{{ path('app_product_index') }}" class="list-group-item list-group-item-action">
                        <i class="fas fa-box-open me-2"></i> Produits
                    </a>

                    <a href="{{ path('app_category_index') }}" class="list-group-item list-group-item-action">
                        <i class="fas fa-tags me-2"></i> Catégories
                    </a>

                </div>
            </div>

            {# ================================================================= #}
            {# 3. LE CONTENU PRINCIPAL                                           #}
            {# ================================================================= #}
            <div id="page-content-wrapper">
                <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom px-4 py-3 shadow-sm">
                    <div class="container-fluid">
                        <span class="navbar-text text-muted fw-bold">
                            Gestion de Stock
                        </span>
                    </div>
                </nav>

                <div class="container-fluid p-4">
                    {% block body %}{% endblock %}
                </div>
            </div>

        </div>

        {# ================================================================= #}
        {# 4. JAVASCRIPT                                                     #}
        {# ================================================================= #}
        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </body>
</html>
