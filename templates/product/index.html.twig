{% extends 'base.html.twig' %}

{% block title %}Liste des Produits{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center my-4">
        <h1 class="text-primary">Liste des Produits</h1>
        <a href="{{ path('app_product_new') }}" class="btn btn-success">
            <i class="fas fa-plus me-1"></i> Créer nouveau
        </a>
    </div>

    <div class="card border-0 shadow-sm mb-4 bg-light">
        <div class="card-body">
            <form action="{{ path('app_product_index') }}" method="get" class="d-flex">
                <input type="text" name="q" class="form-control me-2"
                       placeholder="Rechercher un produit..."
                       value="{{ app.request.query.get('q') }}">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-search"></i> Chercher
                </button>
                {% if app.request.query.get('q') %}
                    <a href="{{ path('app_product_index') }}" class="btn btn-link text-danger ms-2">Effacer</a>
                {% endif %}
            </form>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <table class="table table-hover mb-0 align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Image</th>
                        <th>Titre</th>
                        <th>Prix</th>
                        <th class="text-end pe-4">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for product in products %}
                    <tr>
                        <td>
                            {% if product.image %}
                                <img src="{{ asset('images/' ~ product.image) }}" alt="img" class="rounded" style="width: 50px; height: 50px; object-fit: cover;">
                            {% else %}
                                <span class="badge bg-secondary">No image</span>
                            {% endif %}
                        </td>
                        <td class="fw-bold">{{ product.title }}</td>
                        <td class="text-success fw-bold">{{ product.price }} MAD</td>

                        <td class="text-end pe-3">
                            <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-sm btn-info text-white rounded-circle"><i class="fas fa-eye"></i></a>

                            <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="btn btn-sm btn-warning text-white rounded-circle"><i class="fas fa-pen"></i></a>

                            {% if is_granted('ROLE_ADMIN') %}
                                <form method="post" action="{{ path('app_product_delete', {'id': product.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce produit ?');" style="display: inline-block;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
                                    <button class="btn btn-sm btn-danger rounded-circle ms-1"><i class="fas fa-trash"></i></button>
                                </form>
                            {% endif %}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center py-4 text-muted">Aucun produit trouvé</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
